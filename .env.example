# ── Copy to .env and fill in your values ──────────────────────────

# REQUIRED — Anthropic API key (powers the vulnerability analyzer)
ANTHROPIC_API_KEY=sk-ant-...

# OPTIONAL — OpenAI key (only needed if auditing OpenAI-based targets)
OPENAI_API_KEY=sk-...

# App
DEBUG=false
